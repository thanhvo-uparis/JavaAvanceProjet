package outils;
import constructionEcoles.Agglomeration; 

public class Tester {
	// cette classe va contenir les méthodes qui permettront de tester les algos. Il faut y mettre le contenu de la classe Test
	
	public static Agglomeration AggloGenerateurRandomOperations(int nbVilles, int nbOperations) {
		//classe Test à mettre ici
		
		//temporaire juste pour qu'il n'y ait pas d'erreurs 
		return new Agglomeration() ;
	}
	
	
	public static Agglomeration RandomAggloGenerateur(int nbVilles) {
	//classe classe qui renvoie une Agglomération aléatoire
	
	//temporaire juste pour qu'il n'y ait pas d'erreurs 
		return new Agglomeration() ;
	}
	
	//pour tester la complexité d'un algo en particulier
	public static double testComplexiteAlgo(Agglomeration agg) {
		return 0 ;
	}
	
	// etc etc.
}
